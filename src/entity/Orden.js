class Orden {
  constructor(idOrden, fecha, sucursal, anulada) {
    this.idOrden = idOrden;
    this.fecha = fecha;
    this.sucursal = sucursal;
    this.anulada = anulada;
  }
}

export default Orden;
